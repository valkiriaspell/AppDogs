const { Router } = require('express');
const {} = require("../controllers/dogs");

const router = Router();

router.get("/", getDogs)

router.get("/:name", )

router.get("/{idRaza}", )

router.post("/", )


module.exports = router;
